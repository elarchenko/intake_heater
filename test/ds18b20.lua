local wo = {}

function read(bind, args)
  return 0
end

function setup(sensor_pin)
  return 0
end

function setting(roms, prec)
  return 0
end

wo.read = read
wo.setup = setup
wo.setting = setting

return wo